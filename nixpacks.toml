[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan migrate --force && php artisan storage:link 2>/dev/null; php artisan route:cache && php artisan view:cache && php -S 0.0.0.0:$PORT -t public"
