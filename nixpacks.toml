[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan migrate --force && php artisan db:seed --force && php artisan storage:link 2>/dev/null; php -S 0.0.0.0:$PORT -t public"
